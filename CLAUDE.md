# CLAUDE.md

## コミュニケーション

- 日本語で応答する（コード・変数名・関数名は英語）
- 簡潔に回答し、自明な説明は省略する
- 複雑なタスクは実装前に計画を提示し、承認を得てから着手する

## プロジェクト概要

- 世界遺産の分布・登録動向を可視化するStreamlitアプリ
- 詳細な仕様は `docs/世界遺産可視化アプリ開発計画書.md` を参照する（docs/ はGit管理外・ローカル専用。コミットしない）
- 起動: `streamlit run app/main.py` / テスト: `pytest`

## ルール

- アーキテクチャ原則・コードスタイルの詳細は `.claude/rules/` を参照する（対象パスごとに自動適用）

## Git規約

- Conventional Commits形式、本文は日本語（例: `feat: 地図にマーカー表示を追加`）
- 確認なしに自動コミット・自動pushしない
- `docs/`・`data/raw/`・`data/cache/`・シークレット類はコミットしない（.gitignore設定済み。解除しない）

## 禁止事項

- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない
- APIキー・連絡先メールアドレスをコードに直接書かない（`.env` / `st.secrets` を使用する）
