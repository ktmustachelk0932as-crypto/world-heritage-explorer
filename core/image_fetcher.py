"""Wikipedia REST API呼び出しによる代表画像URL・ライセンス情報の取得。

計画書11章の方針に従う：
- User-Agentに連絡先情報を含める（環境変数 WIKIMEDIA_CONTACT_EMAIL）
- 逐次実行・リクエスト間インターバル（100〜200ms）
- 一時的失敗（タイムアウト・5xx）は指数バックオフで最大3回リトライ
- 恒久的失敗（404等）はリトライせずフォールバック
- タイムアウトを明示的に設定（例：5秒）
"""
